package bet;

import java.util.Arrays;
import java.util.List;

public class betden {
	
	//List<String> listT = Arrays.asList(arrayT);
	//String[] y = listT.toArray(new String[0]);
	//List<String> list = new ArrayList<Sting>(c)
	//Collection<Type> nodup= new HashSet<Type>(c) (veya LinkedHashSet)
	//c.stream().collect(collectors.toSet()))
	//c.stream().map(Person::getName).collect(collectors.toCollection(TreeSet::new))
	//queue element, offer, peek, pool , remove 
	//Queue<Integer> qu = new LinkedList<Integer>()
	//Map<String,Integer> m= new HashMap<String,Integer>()
	

	
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		System.out.print("deneme");

	}

}
